<%= form_with(model: photo, local: true) do |form| %>
  <% if photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :photo_name %>
  </div>

  <div class="field">
    <%= form.label :data %>
    <%= form.text_field :data, id: :photo_data %>
  </div>

  <div class="field">
    <%= form.label :filename %>
    <%= form.text_field :filename, id: :photo_filename %>
  </div>

  <div class="field">
    <%= form.label :mime_type %>
    <%= form.text_field :mime_type, id: :photo_mime_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
